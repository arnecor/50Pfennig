/**
 * features/settlements/components/RecordSettlementSheet.tsx
 *
 * Bottom sheet (shadcn Sheet) for recording a settlement.
 *
 * Pre-populates with the suggested payment from simplifyDebts()
 * but allows the user to adjust the amount.
 *
 * Fields:
 *   - From (member selector, defaults to current user)
 *   - To (member selector)
 *   - Amount (numeric, converted to cents)
 *   - Note (optional text)
 */

// TODO: Implement

export default function RecordSettlementSheet() {
  return null;
}
