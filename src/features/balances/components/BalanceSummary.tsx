/**
 * features/balances/components/BalanceSummary.tsx
 *
 * Displays the net balance for each member of a group.
 * Positive balance: shown in green ("you are owed €X")
 * Negative balance: shown in red ("you owe €X")
 * Zero balance: shown as settled
 *
 * Also renders the DebtSimplifier: the minimal list of payments
 * needed to settle all debts, with a "Record settlement" CTA on each.
 *
 * Data source: useGroupBalances() — no direct fetches.
 * Amount display: always via <MoneyDisplay> component.
 */

// TODO: Implement

export default function BalanceSummary() {
  return null;
}
