/**
 * pages/ExpenseFormPage.tsx
 *
 * Routes:
 *   /groups/:groupId/expenses/new                 (create mode)
 *   /groups/:groupId/expenses/:expenseId/edit     (edit mode)
 *
 * Determines create vs edit mode from the presence of expenseId param.
 * Passes existing expense data to ExpenseForm when in edit mode.
 */

// TODO: Implement

export default function ExpenseFormPage() {
  return null;
}
