/**
 * pages/SettlementsPage.tsx
 *
 * Route: /groups/:groupId/settlements
 *
 * Full history of settlements for a group, newest first.
 * Each item shows who paid whom, the amount, and an optional note.
 * Delete settlement action (with confirmation).
 */

// TODO: Implement

export default function SettlementsPage() {
  return null;
}
