<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Dark mode: solid dark background + light (white) icons visible on it.
         These XML values are the fallback during the splash screen phase,
         before the JS StatusBar plugin takes over. -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:statusBarColor">@color/statusBarBackground</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

</resources>
